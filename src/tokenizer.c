//Start of tokenizer.c
//WIP: functions listed in tokenizer.h

#include <stdio.h>

int space_char(c);
  {
    return (c == '\t');
  }

int non_space_char(c);
  {
    return c == ~('\s') | ~(c == '\t');
  }

char *token_start(char *str);
  {

    if 
    return 0;
  }

